import {  Options } from 'sequelize';

const config: Options = {
        username: 'root',
        password: 'q1w2e3',
        database: 'db_teste',
        host: 'localhost',
        port: 3306,
        dialect: 'mysql'
    
};

export = config;
